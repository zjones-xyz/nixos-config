# PLACEHOLDER — not yet sops-encrypted. Replace before deploying hopper.
#
# After hopper's first boot, add its age key to .sops.yaml (see notes there),
# then create the real encrypted file with:
#   sops secrets/hopper.yaml
#
# Expected secrets (keys referenced by the hopper modules):
#   cloudflare:
#     apiToken: <CF token>               # Cloudflare API token (DNS edit perms)
#                                        # shared with memory-alpha's token
#   tailscale:
#     authKey: tskey-auth-...            # reusable/ephemeral key from admin console
#   nut:
#     upsmonPassword: <upsmon password>  # arbitrary, must match upsd user
#   beszel:
#     agentKey: KEY=ssh-ed25519 ...      # hub public key, as an env line
#   speedtest-tracker:
#     appKey: APP_KEY=base64:...          # `openssl rand -base64 32`
#
# This plaintext placeholder will NOT decrypt at activation — sops-nix will
# error until you replace it with a properly encrypted file.
