# PLACEHOLDER — not yet sops-encrypted. Replace before deploying hamilton.
#
# After hamilton's first boot, add its age key to .sops.yaml (see notes there),
# then create the real encrypted file with:
#   sops secrets/hamilton.yaml
#
# Expected secrets (keys referenced by the hamilton modules):
#   tailscale:
#     authKey: tskey-auth-...            # reusable/ephemeral key from admin console
#
# This plaintext placeholder will NOT decrypt at activation — sops-nix will
# error until you replace it with a properly encrypted file.
